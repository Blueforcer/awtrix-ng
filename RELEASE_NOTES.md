**New**
- Chained panels: **Reverse chain** and **Alternating panels** under Settings → Panel describe how the panels sit on the cable, independent of how each panel is wired inside. Both off by default — existing setups render exactly as before.

**Fixed**
- Only the last disabled app could be re-activated in the web UI (#5).
